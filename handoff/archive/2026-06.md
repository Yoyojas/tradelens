# HANDOFF 归档 · 2026-06（只读，原文未改动；当前状态见 ../../HANDOFF.md）

## [CC → Cowork] 2026-06-29 · i18n 扩展：地球下拉 + 7 语言完成

**UI：LanguageMenu 取代 LanguageToggle**
- 顶栏地球图标按钮（内联 SVG）+ popover 下拉，列各语言**母语名**（English / 简体中文 / Español / Français / Deutsch / 日本語 / 한국어），当前语言打 ✓。
- 点选即 `setLang`＋写 localStorage＋关闭；点外部关闭；键盘可达（↑/↓/Home/End 移动、Enter/Space 选择、Esc 关闭并把焦点还给按钮）；`role=menu` + `menuitemradio` + `aria-checked`。样式进 `css/layout.css`，外部 CSS、无 inline。旧 `LanguageToggle.jsx` 已删除。

**语言注册表**
- 新增 `src/i18n/languages.js`：`LANGUAGES=[{code,label,dict}]` + `DICTS`/`LANGUAGE_CODES`/`DEFAULT_LANG`/`DATE_LOCALES`。加语言只改这一处。
- 新增 5 部字典 `es/fr/de/ja/ko`（加既有 en/zh）。未做 RTL（按规格留作未来）。
- `LanguageContext` 改为从注册表取 `DICTS`；`setLang` 忽略未知 code；`t()` 回退链 = 所选语言 → en → key。

**翻译范围**
- **界面外壳 + 受控词汇**（category/market/tag/risk）：7 种语言全翻。
- **playbook 自由文本**（name/summary/description/rules）：仅 en+zh 全翻；`tField` 已泛化为查 `${field}_${lang}`，es/fr/de/ja/ko 自动回退英文（不空白）。将来加 `name_es` 等即可逐步补全。

**格式化**：日期按 locale（`setDateLocale` 用 `DATE_LOCALES` 映射 en-US/zh-CN/es-ES/fr-FR/de-DE/ja-JP/ko-KR）；金额恒 `$`。

**自检**
- 7 部字典键集与 en **完全一致：各 214 个 key，零缺失/零多余**（脚本校验）。
- **占位符校验通过**：每条含 `{n}/{ticker}/{date}` 等的文案在所有语言中占位符集合与 en 一致，无丢失 → 插值不会断。
- `npm run build` 通过。

**需你/母语者复核的金融术语（不确定项，已标出）**
1. **P&L 缩写本地化**：我按各语言习惯译了——es `P&G`、fr `P&P`、de `G&V`、ja/ko `损益/손익`。但不少交易者更认英文 **`P&L`**。**建议**：是否统一保留 `P&L`（最稳）？等你定，一行就能改。
2. **德语保留英文术语**：`Momentum / Mean Reversion / Value` 在德语金融语境常直接用英文，我 de 里保留了英文；其余（Makro/Trend/Ausbruch/überverkauft 等）已德化。如需全德化告知。
3. **Event-Driven**：es「Por eventos」/ fr「Événementiel」/ de「Ereignisgetrieben」/ ja「イベントドリブン」/ ko「이벤트 드리븐」——可接受？
4. 其余术语（胜率/盈亏比/持有期/集中度/超卖/突破等）按标准金融词翻译，自认稳妥。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 顶栏点地球图标 → 下拉 7 语言，当前项打 ✓；选 Français/日本語/한국어 等 → 界面外壳与类别/市场/标签/风险即时切换；键盘 ↑↓+Enter 也能选，Esc 关闭。
2. 选非 en/zh 语言 → playbook 卡片标题/摘要/描述/规则**回退英文**（不空白），其余界面为该语言。
3. 日期随 locale 变（如 ja「2026/2/1」、de「1.2.2026」），金额仍 `$`。
4. 刷新 → 语言保持。

**无阻塞问题**（仅上面 4 个术语复核点，默认维持现状，等你要改再说）。等你验收。

---

## [Cowork → CC] 2026-06-29 · i18n 扩展：多语言 + 地球下拉切换

把语言切换从 EN | 中文 分段控件换成**地球图标 + 下拉菜单**，并扩展支持的语言。

**UI：LanguageMenu（取代现有 LanguageToggle）**
- 顶栏放一个地球图标按钮（🌐 或等价 SVG），点击展开下拉，列出各语言的**母语名**（English / 简体中文 / Español / Français / Deutsch / 日本語 / 한국어），当前语言打勾。
- 点选即 `setLang` 并写 localStorage、关闭菜单；点外部或 Esc 关闭；键盘可达（方向键/Enter/Esc）。轻量 popover，样式进外部 CSS、无 inline。

**语言注册表**
- `src/i18n/languages.js`：`[{ code, label(母语名), dict }]` 集中管理。新增字典 `es/fr/de/ja/ko`（加现有 en/zh）。
- 先**不做 RTL**（阿拉伯语/希伯来语），避免镜像布局，留作未来。

**每语言翻译范围**
- **界面外壳 + 受控词汇**（category/market/tag/riskLevel）：所有语言都翻，用准确金融术语。
- **playbook 自由文本**（name/summary/description/rules）：只保留 en + zh 全翻；其它语言用 `tField` 回退英文（不空白）。以后可加 `name_es` 等字段补全。
- `t()` 回退链：所选语言 → 英文 → key。任何缺失回退英文，绝不露空白或 key。各语言键集要与 en 对齐（沿用你现有的键集一致性自检）。

**格式化**
- 日期按当前 locale 用 `Intl.DateTimeFormat`（沿用 `setDateLocale`，扩展到 es-ES/fr-FR/de-DE/ja-JP/ko-KR）；金额保持美元 `$`。

**验收**
- 地球下拉列出全部语言、当前项打勾、点选即切换并持久化；界面外壳与类别/市场/标签/风险在各语言下正确；未翻字符串回退英文；非 en/zh 的 playbook 长文回退英文；刷新保持；`npm run build` 通过；各语言键集与 en 一致。
- 金融术语谨慎，不确定的在 HANDOFF 标出供复核。完成后顶部追加 `[CC → Cowork]`。

---

## [CC → Cowork] 2026-06-29 · i18n 中英双语切换完成

**按规格实现**
- 新建 `src/i18n/LanguageContext.jsx`（`lang`/`setLang`/`t`，`useLang()`）、`en.json`、`zh.json`、`components/LanguageToggle.jsx`。
- `lang` 默认读 `localStorage['tradelens.lang']`，无则 `'en'`；`setLang` 写回 localStorage（刷新不丢）。
- `t(key, vars)`：当前语言 → 英文回退 → key 本身；支持 `{var}` 插值。
- `LanguageProvider` 包在最外层（`main.jsx`：Language > Auth > Data > Router）。
- `LanguageToggle`（`EN | 中文` 分段控件）放进顶栏，紧挨用户名/Log out；样式进 `css/layout.css`，外部 CSS、无 inline。

**字符串抽取**：导航、认证、Library、Journal、Reports、Admin、确认弹窗、空状态、指标卡标签、图表标题/轴/图例/tooltip、`validation.js` 校验报错、`AuthContext` 登录/注册报错全部走 `t()`。键按视图分命名空间（`nav/auth/library/journal/reports/admin/validation/common`）。
- `validation.js` 与 `AuthContext` 改为返回**i18n key**，由组件 `t()` 翻译（保持纯函数/无语言耦合）。

**范围边界**（严格遵守）：未翻译 playbook 种子内容（name/summary/description/rules）、category/market 等数据值、用户 notes、ticker。风险等级 Low/Medium/High 作为 UI 分类**有**翻译（徽章/筛选/编辑器下拉显示中文，但筛选 value 仍用英文以匹配数据）。

**格式化**：金额恒为美元 `$`（不随语言变）；日期在 `utils/format.js` 用 `Intl`，`setDateLocale` 按语言切 `zh-CN`/`en-US`，`LanguageProvider` 在初始化与 lang 变化时调用（首屏即正确，无闪烁）。

**自检**
- en/zh 键集完全一致：各 160 个 key，无缺失/多余 → zh 模式不会回退英文。
- `npm run build` 通过。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 顶栏右上 `EN | 中文`，点「中文」→ 全界面外壳即时变中文（导航/表单/表头/指标卡/图表标题与图例/弹窗）；playbook 名称与摘要、ticker、notes 保持原文。
2. 切中文后日期变 `zh-CN` 格式（如 2026年2月1日），金额仍是 `$`。
3. 刷新页面 → 语言保持中文。
4. 触发校验（如 Journal 空提交）与登录报错 → 报错文案也是中文。
5. 切回 EN 一切复原。

**验收对照**：切换即时生效 ✓、刷新保持 ✓、无残留英文（数据内容除外）✓、无可见 key 回退 ✓、build 通过 ✓。

**无待决问题。** 等你验收。

---

## [CC → Cowork] 2026-06-29 · i18n 增补：受控词汇（类别/市场/标签）查表翻译完成

**做法（按你的规格）**
- en.json / zh.json 新增三个以**英文原值为键**的命名空间：`category.* / market.* / tag.*`（riskLevel 之前已在 `risk.*` 字典里，统一管理）。
- `LanguageContext` 新增 `tVocab(namespace, value)`：直接两级查表（不按 `.` 切分，兼容 "US Equities"/"Event-Driven"/"mean-reversion" 这类含空格或连字符的值），**查不到回退原值**（不是 key）——admin 新建的未知类别/标签照常显示原文，绝不空白。
- 显示处全部走 `tVocab`：playbook 卡片 chip、详情弹窗、筛选下拉选项 label、admin 表格的类别/市场/标签列。
- **底层值保持英文**：筛选下拉 `<option value>` 仍是英文原值，`p.category === filters.category` 匹配逻辑零改动；只翻可见 label。
- Library 搜索 haystack 并入译文，中文也能搜（如「动量」「突破」「美股」）。

**采用的译法**（与你给的一致）
- 类别：Momentum=动量、Mean Reversion=均值回归、Value=价值、Event-Driven=事件驱动、Macro=宏观、Trend=趋势
- 市场：US Equities=美股、Crypto=加密货币、ETFs=ETF
- 风险：Low=低风险、Medium=中风险、High=高风险（沿用既有 `risk.*`）
- 标签：breakout=突破、volume=成交量、mean-reversion=均值回归、trend=趋势、earnings=财报、value=价值、macro=宏观、crypto=加密、oversold=超卖

**保持不翻**：playbook 的 name/summary/description/rules、用户 notes、ticker。

**自检**
- en/zh 键集仍完全一致（各 178 个 key，无缺失/多余）。
- 种子里出现的全部 category/market/tag 值在 zh 字典均有对应，无意外回退。
- `npm run build` 通过。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 切中文 → Library 卡片上的类别/市场 chip、`#标签`、详情弹窗、筛选下拉、Admin 表格全部显示中文（如 动量 / 美股 / #突破 / 中风险）。
2. 筛选仍正确：选「动量」类别 → 只剩动量策略（底层 value 仍是 `Momentum`）。
3. 中文搜索框输「突破」或「美股」→ 能命中对应策略。
4. Admin 新建一个写自定义类别（如 `Swing`）的策略 → 中文模式下该格回退显示原文 `Swing`，不空白。

**无待决问题。** 等你验收。

---

## [CC → Cowork] 2026-06-29 · i18n 增补 2：playbook 内容双语完成

**数据层（双语字段，不进 UI 字典）**
- `playbooks.json` 8 条种子每条新增 `name_zh / summary_zh / description_zh / rules_zh`（rules_zh 为字符串数组，逐条对应英文 rules）。译名/摘要用你给的版本，description/rules 按相同金融语境翻译。
- `services/api.js` 加 `SEED_VERSION='2'`：检测到旧版本即**只**重置 `tradelens.playbooks` 让其从新种子重载（用户交易/账号/采用关系不动）。**解决了之前 localStorage 里旧 playbook 无 zh 字段的陈旧问题**——你直接刷新即可看到中文，无需手动清缓存。

**取值助手**
- `LanguageContext` 新增 `tField(obj, field)`：zh 模式且 `field_zh` 非空 → 返回中文（字符串或数组）；否则回退英文 `field`。空字符串/空数组都算缺失 → 回退。

**应用处**（playbook 名称/摘要/描述/规则全部走 `tField`，中英一致不混杂）
- Library 卡片（标题、摘要）、详情弹窗（标题、摘要、描述、规则）。
- Admin 表格名称列。
- Reports 的 playbook 选择器、Journal 录入下拉、Journal 历史筛选下拉、交易历史表里的 playbook 名。
- Library 搜索 haystack 并入当前语言的 name/summary（中文标题可搜，如「突破动量」）。

**Admin 编辑器**
- 新增三个**可选**中文字段：名称(中)/摘要(中)/描述(中)，附说明"留空则中文下回退英文"。保存即写入 `name_zh/summary_zh/description_zh`；编辑已有策略时不会丢失原有 `rules_zh`（patch 合并保留）。

**仍不翻**：用户输入的交易 notes、ticker，原样不动。

**自检**
- en/zh 键集一致（各 182，含新增 4 个 admin 中文字段键）。
- 8 条种子 playbook 的 4 个 zh 字段齐全，`rules_zh` 与 `rules` 条数一一对应。
- `npm run build` 通过。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 切中文 → Library 卡片标题变「突破动量」「趋势跟踪」等，摘要中文；点开详情，描述与规则均中文。
2. Journal/Reports 里的 playbook 名也跟着中文；交易历史表 playbook 列中文。
3. 中文搜索框输「均值回归」「板块」→ 命中对应策略。
4. Admin 编辑某策略，清空「摘要(中)」→ 该策略中文下摘要回退英文，不空白；新建策略只填英文 → 中文下整条回退英文。
5. 切回 EN 全部恢复英文。

> 注：旧 localStorage 已通过 SEED_VERSION 自动重载 playbook；若你之前在 Admin 建过自定义策略，这次迁移会被新种子覆盖（仅此一次）。

**无待决问题。** 等你验收。

---

## [CC → Cowork] 2026-06-29 · IB Gateway 只读实时连接（Flask 后端 + 前端实时模式）完成

**后端 `tradelens/backend/`（Flask + ib_insync，全部只读）**
- `app.py`：Flask + flask-cors。端点 `GET /api/health`、`/api/ibkr/status`、`/api/ibkr/account`、`/api/ibkr/positions`、`/api/ibkr/executions`。status 永不硬失败（网关没开返回 `connected:false,error`），数据端点连不上返回 503+error。
- `ib_service.py`：单例连接管理。**专用后台 asyncio 事件循环线程**里创建 `IB()`，所有调用经 `run_coroutine_threadsafe` marshal 到该循环，`threading.Lock` 串行化并发请求。连接 `connectAsync(..., readonly=True)`。映射：accountSummary、positions（symbol/position/avgCost…）、executions（`reqExecutionsAsync` 的 fills 归一化）。
- `pairing.py`：**纯函数 FIFO 配对**——按 symbol 把逐笔成交配成 round-trip 交易（多/空都处理），佣金按成交股数比例分摊进 `fees`，未配平的剩余作为持仓中；`externalId = openExecId::closeExecId`（持仓中用 openExecId）。
- `config.py`：`IB_HOST/IB_PORT(默认4001)/IB_CLIENT_ID/超时/FLASK_PORT(5001)/CORS_ORIGINS` 全走 env/`.env`，不硬编码。`IB_READONLY=True` 硬写死。
- `README.md`：IB Gateway 必需设置（Enable Socket Clients、**Read-Only API**、Trusted IP 127.0.0.1、端口 4001 live/4002 paper）、venv 安装、运行、坑（clientId 冲突、端口搞错、asyncio）。
- `.env.example`、`backend/.gitignore`（忽略 `.env`/venv/`__pycache__`/任何账户导出）。

**只读铁律**：全后端 grep 无 `placeOrder/cancelOrder/transfer/.buy(/.sell(` 等任何下单调用（只在注释里声明只读）。

**前端实时模式**
- `services/ibkr.js`：唯一与后端通信处，base URL 走 `VITE_IBKR_API`（默认 `http://127.0.0.1:5001`）；区分「后端没启动(network error)」与「请求失败」。
- 新页 `pages/ConnectPage.jsx`（路由 `/connect`，顶栏「Live/实时」链接，所有登录用户可见）：连接状态条 + 账户摘要卡 + 持仓表 + 成交→可导入交易预览 + 一键导入。
- `DataContext.importTrades(incoming)`：按 `source+externalId` **去重**，重复导入不重复；手工交易现在标 `source:'manual'`。
- **隐私**：账户/持仓/成交仅存于组件内存，**不写 localStorage**；只有用户点「导入」的交易记录才落盘（不含余额）。文案全走 i18n（en/zh，新增 `connect.*` + `nav.connect`）。
- CORS 允许 `localhost:5173`。

**架构**：IB Gateway（本地、已登录、Read-Only API）← 127.0.0.1 socket ← Flask(`ib_insync`) → REST/JSON → React `/connect`。

**自检（我能在此环境验的）**
- 后端 4 个 .py `py_compile` 通过。
- `pairing.py` 用合成成交单测：AAPL 买100→卖60/卖40 配成两笔多头已平仓、佣金按比例分摊；TSLA 卖50→买30 配成空头已平仓 + 剩 20 持仓中。FIFO/多空/分摊均正确。
- 前端 `npm run build` 通过；en/zh 键集一致（各 214）。

**我无法在此环境验的（需要你的机器）**
- 真连 IB Gateway：本环境没有 IB Gateway，也没装 `ib_insync`（需联网 pip）。**三个数据端点对真实账户的返回、字段名（尤其 executions/commissionReport）需你本地跑通确认**。ib_insync 的属性名我按文档写（`ex.side` 为 BOT/SLD 已兼容、`f.commissionReport.commission`），但建议你首次连真账户时核对一遍 positions/fills 字段。

**怎么跑（你本地）**
```bash
# 1) 终端 A：后端（先在 IB Gateway 开 Read-Only API、端口 4001）
cd tradelens/backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env        # 如用 paper 改 IB_PORT=4002
python app.py               # http://127.0.0.1:5001
# 2) 终端 B：前端
cd tradelens && npm run dev
# 3) 浏览器 /connect → Connect/Refresh → 看状态/账户/持仓 → Import N trades → 去 Journal/Reports 看导入的交易
```

**遇到的 IB Gateway 连接坑（已写进 backend/README）**
- 端口：Gateway live 是 **4001**（不是 TWS 的 7496）；status 端点会回显尝试的 host/port，便于排错。
- `clientId` 冲突：上一个 API 连接没断会拒连，换 `IB_CLIENT_ID` 或重启网关。
- asyncio：必须经后台事件循环线程，不能在 Flask 请求线程里直接调 ib_insync（已封装好）。
- macOS：Flask 用 **5001**（5000 被 AirPlay 占）。

**待你拍板/确认的点**
1. 真账户字段核对（见上「无法在此环境验」）——你本地连通后如有字段名出入，告诉我具体报错/JSON，我据此修 `ib_service.py` 的映射。
2. `/connect` 入口我放成了独立顶栏页（而非塞进 Journal 的导入区），更干净也好录 demo；若你坚持要在 Journal 内，我再挪。
3. 导入的交易 `playbookId` 置空（券商不知道策略），用户可在 Journal 里补挂。可接受？

下一步（若需要）：完整历史的 Flex 上传解析（另一条规格），与本实时通路互补。

---

## [Cowork → CC] 2026-06-29 · 新任务：IB Gateway 实时连接（Flask 后端，M2 起点）

用户用 IBKR **live 账户**，并在 IB Gateway 开启 **Read-Only API**。实现一个连接 IB Gateway 读取账户数据的真实通路。浏览器无法直连 IB Gateway（socket 二进制协议），必须经本地 Python 后端。这个后端就是 capstone 的 Flask 后端起点（Milestone 2）。

**架构**
IB Gateway（用户本地、已登录、Read-Only API）← 127.0.0.1 socket ← Flask 后端（`ib_insync`）→ REST/JSON → React 前端。

**后端：`tradelens/backend/`（Flask）**
- 依赖：`flask`、`flask-cors`、`ib_insync`。
- 连接：`ib_insync` 连 `127.0.0.1`，端口默认 **4001**（IB Gateway live；可配置，paper 为 4002），`clientId` 可配置。连接参数走环境变量/配置文件，不硬编码。
- `ib_insync` 是 asyncio，在 Flask 里请用专用后台事件循环线程或 `util.run()` 的同步用法，避免事件循环冲突；连接用单例 + 锁，按需复用。
- 端点（**全部只读**）：
  - `GET /api/ibkr/status` —— 是否连上、网关版本。
  - `GET /api/ibkr/account` —— accountSummary（净值、现金、币种等）。
  - `GET /api/ibkr/positions` —— 当前持仓（symbol、position、avgCost…）。
  - `GET /api/ibkr/executions` —— 当日/近期成交（`ib.fills()`），归一化。
- **只读铁律**：后端只调读取类 API，绝不出现下单/改单/转账调用。代码层面也不写这类函数。
- 映射成 TradeLens 形状：executions → trades（按 symbol FIFO 配对，commission 进 fees，复用已有配对逻辑）；positions → 持仓中条目（entryPrice=avgCost）。`source='broker:ibkr'`，`externalId` 取 IBKR execId。
- 启动与配置写进 `backend/README.md`，含 IB Gateway 必需设置（Enable Socket Clients、Read-Only API、Trusted IP 127.0.0.1、端口）。

**重要：数据落地与隐私（必须遵守）**
- 真实账户数据**默认只在内存/会话**，不自动写入 localStorage 持久层（与 mock 数据隔离）。
- `.gitignore` 增加：任何真实导出、账户 dump、`backend/.env`、凭证类文件。**绝不 commit 真实账户数据。**
- 数据全量历史的局限：IB Gateway API 只回近期成交；完整历史交易仍走 Flex 上传（另一条规格），二者互补。

**前端**
- Journal 的导入里增加"Connect IBKR (live via IB Gateway)"模式：调本地后端，显示连接状态、账户摘要、持仓，可把成交导入为交易（预览+去重，与 Flex 路径一致）。后端地址可配置；处理后端未启动/网关未连接的报错提示。新文案走 i18n（en/zh）。
- CORS：flask-cors 允许 `localhost:5173`。

**验收**
- IB Gateway（live, Read-Only）运行时，启动 Flask 后端，三个端点返回真实 JSON；前端显示账户/持仓并能导入成交（去重、持久化仅针对导入的交易记录而非账户余额）；只读、无任何下单代码；真实数据不入 git；前端 `npm run build` 通过、后端可独立启动。
- 完成后 HANDOFF 顶部追加 `[CC → Cowork]`，附后端启动方式与遇到的 IB Gateway 连接坑。

---

## [Cowork → CC] 2026-06-29 · 券商导入改为 IBKR 专属（取代下方通用 mock 规格）

用户用 IBKR。第一个 provider 就做 IBKR，基于 IBKR **Flex Query / Flex Web Service** 的真实数据格式。下方那条"通用 mock"规格的**适配器架构仍然采用**，只是首个适配器具体化为 IBKR。

**真实可用的核心：解析 IBKR Flex 报表**
- 适配器 `services/brokers/ibkr.js`。
- M1 的"连接"提供两种来源（都在浏览器内完成，不碰凭证）：
  1. **上传**：用户在 IBKR 账户管理生成 Activity/Trades Flex 报表（XML 或 CSV）后上传，TradeLens 解析其**真实交易**导入。
  2. **演示样本**：内置一份样本 IBKR Flex XML，供没有账户时演示，UI 标注"演示样本"。
- 解析：用浏览器内置 `DOMParser` 解析 Flex 的 `<Trade>` 节点（无需新依赖）。典型字段映射（请对照一份真实 Flex 导出确认属性名）：
  - `symbol` → ticker；`buySell`（BUY/SELL）→ 用于配对方向；`quantity` → quantity；`tradePrice` → 价格；`tradeDate` → 日期；`ibCommission`/`commission` → fees；`assetCategory`（先只取 `STK`，其它类别本期跳过并在预览里说明）；`tradeID` → externalId。
  - IBKR 给的是**逐笔成交（execution）**，不是 round-trip。请按 symbol 做 **FIFO 配对**，买卖配成一笔已平仓交易（entryPrice/exitPrice/openDate/closeDate/合计 fees），未配平的剩余做持仓中。配对逻辑用纯函数、可单测。
- trade 增补字段：`source`（`'manual'` | `'broker:ibkr'`）、`externalId`（来自 IBKR tradeID，可为合并后的多笔 id）。用于去重，重复导入不重复。

**留作后端的部分（写进 README 路线图，仅留接口位，本期不实现）**
- 实时 Flex Web Service 拉取：`token` + `Flex Query ID` 经 HTTPS 调用，受 CORS 与 token 安全限制，需后端代理。前端不存 token、不碰账户密码。

**导入 UI**
- Journal 加"Connect IBKR / 导入 IBKR 交易"按钮 → 复用 `Modal`：选择上传报表或用演示样本 → 解析 → **预览表**（标出已存在的、可勾选导入）→ 确认导入。导入后进历史与 Reports、落 localStorage、去重生效、手动录入不受影响。
- 全部新文案走 i18n（en/zh）。

**验收**
- 上传一份 IBKR Flex 报表（或用演示样本）→ 预览解析出的交易（FIFO 配对正确、commission 进 fees、STK 过滤）→ 选择导入 → 出现在历史和 Reports，externalId 去重，刷新保留；手动录入不受影响；中英文案完整；`npm run build` 通过。
- 完成后 HANDOFF 顶部追加 `[CC → Cowork]`；FIFO 配对或字段映射有不确定处一并提出，并附上你用的样本 Flex XML 结构。

---

## [Cowork → CC] 2026-06-29 · 新任务：券商连接导入（适配器架构 + 模拟/沙盒连接）

在 Journal 加"连接券商导入交易"功能，与手动录入并存。**注意约束**：本里程碑无后端、不碰真实资金账户凭证，所以"连接"这一环用**模拟/沙盒适配器**，UI 明确标注"演示连接（模拟数据）"。重点是把**真实架构**建出来，将来接真实券商/纸面交易 API 只需新增一个适配器。

**架构（关键）**
- 新增 `services/brokerService.js`，定义统一适配器接口：`listProviders()`、`connect(providerId, opts)`、`fetchTrades(connection)`、`disconnect()`。它是 manual 之外的另一个导入来源，导入的交易最终仍走与 `addTrade` 一致的写入路径。
- 适配器实现：
  - `adapters/mockBroker.js` —— 模拟连接（带一点延迟），返回一组通用"券商订单/成交"格式的样本数据。无任何凭证。**默认用这个。**
  - 预留 `adapters/paperApi.js` 的接口位（可选/将来）：对接纸面交易/沙盒 API，用用户输入的沙盒 key（仅 session、不持久化、绝不处理真实资金账户密码）。本次可只留 TODO 占位，不实现。
- 映射层：把券商成交数据归一化成 TradeLens 的 trade 形状（ticker/side/quantity/entryPrice/exitPrice/openDate/closeDate/fees）。买卖配对成一笔已平仓交易、未平仓买入成持仓中。mock 数据可直接给配好对的交易，简化配对逻辑。

**交易字段增补（向后兼容）**
- trade 加可选 `source`（`'manual'` | `'broker:<provider>'`，默认 manual）和 `externalId`（券商侧唯一 id，手动为 null）。用于去重。

**导入 UI**
- Journal 录入区旁加"Connect brokerage / 连接券商"按钮，打开复用的 `Modal`。
- 流程：选择 provider → （模拟）连接 → 拉取交易 → **预览表**（与提案 CSV 导入一致的"导入前预览"理念）：列出抓到的交易，标出已存在的（按 `externalId` 去重）、可勾选要导入的 → 确认导入。
- 导入后交易进 Journal 历史与 Reports，落 localStorage，刷新保留；重复导入不产生重复（externalId 去重）。
- 文案走 i18n（en/zh 都要）。"演示连接（模拟数据）"的标注要显眼。

**安全与诚实边界**
- 不收集、不存储任何真实券商密码；只用模拟或沙盒；连接来源在 UI 明确标注为演示/沙盒。
- 在 README 增补一段"未来：真实券商/OAuth 同步需后端"的说明，作为路线图。

**验收**
- 点连接 → 预览样本交易 → 选择导入 → 出现在历史和 Reports，去重正确，持久化；手动录入不受影响；中英文案完整；UI 标注为演示连接；`npm run build` 通过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`，如配对/字段有不确定处一并提出。

---

## [Cowork → CC] 2026-06-29 · i18n 增补 2：playbook 内容也要双语（修正之前的范围）

之前让 playbook 的 name/summary/description/rules 保持英文，现在修正：这些也要支持中文。对中文用户，卡片标题和摘要是最显眼的内容，不能是英文。

**做法：给数据加双语字段，不进 UI 字典**
- 在 playbooks.json 每条加 `name_zh`、`summary_zh`、`description_zh`、`rules_zh`（rules_zh 为字符串数组）。
- 加一个取值助手，例如 `localized(playbook, field, lang)`：`lang==='zh'` 且存在 `field_zh` 时返回中文，否则回退英文 `field`。卡片、详情弹窗、admin 表格都走它。
- admin 的 PlaybookEditor 增加**可选**中文字段（name_zh / summary_zh / description_zh）；留空则该 playbook 在中文下回退英文。
- 搜索：把当前语言的 name/summary 并入 Library 搜索匹配串，中文可搜。

**仍不翻**：用户自己输入的数据（交易 notes、ticker）保持原样，绝不程序改写。

**8 个种子 playbook 的中文译名 + 摘要（请用这些）**
- Breakout Momentum → 突破动量｜在放量突破关键阻力位时买入。
- Mean Reversion Swing → 均值回归波段｜在短期极端价位回归均线时反向介入。
- Trend Following → 趋势跟踪｜借助均线多头排列顺势持有上升趋势。
- Earnings Gap → 财报跳空｜在财报发布后的头一两个交易日内交易其反应。
- Dividend Value Hold → 股息价值持有｜长期持有被低估的高股息股票，等待价值回归。
- Crypto Momentum → 加密动量｜在流动性高的加密主流币上做动量延续。
- Sector ETF Rotation → 板块 ETF 轮动｜按月轮动到表现最强的板块 ETF。
- Oversold Bounce → 超卖反弹｜在原本强势的标的中买入短期超卖回调。

`description_zh` 和 `rules_zh` 较长，请按相同的专业金融语境翻译，风格与上面的摘要一致。

**验收**：中文下卡片标题、摘要、详情描述、规则均为中文；英文下保持英文；缺中文字段时回退英文不留空；用户输入的 notes/ticker 不变；`npm run build` 通过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`。

---

## [Cowork → CC] 2026-06-29 · i18n 增补：翻译受控词汇（类别/市场/标签）

卡片上的 category、market、tags chip 目前仍是英文。把它们也纳入翻译。它们属于**受控词汇（固定枚举值）**，不是自由文本，处理方式和已翻好的 riskLevel 一致。

**做法**
- 在 en.json / zh.json 增加按**英文原值做键**的查表：`category.* / market.* / tag.*`（riskLevel 已处理，若是写死的也一并移进字典统一管理）。
- 凡显示这些值的地方都走查表翻译：playbook 卡片、详情弹窗、筛选下拉的选项、admin 表格。
- **底层值保持英文**：筛选下拉 `<option value>` 仍用英文原值（保证 `p.category === filters.category` 匹配不变），只翻可见 label；卡片 chip 只翻显示。
- **回退**：查不到译文就显示原值（admin 新建的新类别/标签不会变空白）。
- 可选优化：把译文也并入 Library 搜索的匹配串，这样中文也能搜到。

**保持不翻**：playbook 的 name / summary / description / rules、用户 notes、ticker。

**推荐译法**（请用这些，符合金融语境）
- category：Momentum=动量，Mean Reversion=均值回归，Value=价值，Event-Driven=事件驱动，Macro=宏观，Trend=趋势
- market：US Equities=美股，Crypto=加密货币，ETFs=ETF
- riskLevel：Low=低风险，Medium=中风险，High=高风险
- tag：breakout=突破，volume=成交量，mean-reversion=均值回归，trend=趋势，earnings=财报，value=价值，macro=宏观，crypto=加密，oversold=超卖

**验收**：切到中文后，卡片/详情/筛选/admin 里的类别、市场、标签、风险均显示中文；筛选仍能正确过滤；未知值回退显示原文；`npm run build` 通过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`。

---

## [Cowork → CC] 2026-06-29 · 新任务：界面中英双语切换（i18n）

在右上角加一个语言切换（English / 简体中文）。方案：轻量自建 i18n，用 Context + 字典，不引第三方库（保持与现有 Auth/Data Context 一致）。

**要建的文件**
```
src/i18n/LanguageContext.jsx   // lang 状态 + setLang + t()
src/i18n/en.json               // 英文字典
src/i18n/zh.json               // 简体中文字典
src/components/LanguageToggle.jsx
```

**LanguageContext 行为**
- `lang` 默认读 `localStorage['tradelens.lang']`，没有则 `'en'`；`setLang(l)` 写回 localStorage（与应用其它持久化一致，刷新不丢）。
- `t(key)`：按当前语言查字典，缺失回退英文、再回退 key 本身。需要时支持简单 `{var}` 插值。
- 用 `useLang()` 暴露 `{ lang, setLang, t }`。
- 在 `main.jsx` 用 `LanguageProvider` 包住应用（放在较外层，让所有页面可用）。

**LanguageToggle**
- 放进 `AppLayout` 顶栏，紧挨用户名/Log out。
- 分段控件 `EN | 中文`，当前语言高亮，点了即时切换。样式进 `css/layout.css`，外部 CSS、无 inline。

**字符串抽取**
- 把各组件里写死的界面文案换成 `t('...')`。键按视图分命名空间：`nav.* / auth.* / library.* / journal.* / reports.* / admin.* / common.*`。
- 覆盖：导航、按钮、标题/副标题、表单标签与占位、`validation.js` 的校验报错、空状态、指标卡标签、图表轴/图例标签、admin 文案、确认弹窗文案。
- 中文翻译要自然、符合金融/产品语境（如 Win rate=胜率、Adopt=采用、Playbook=策略、Holding period=持有期、Concentration=仓位集中度）。

**范围边界（重要）**
- 只翻"界面外壳"。**不要**翻译 playbook 种子内容（name/summary/description/rules）、用户 notes、ticker 这些数据内容，保持原样。

**格式化**
- 金额保持美元 `$`（标的为美股、以美元计价，不随语言变）。
- 日期按当前语言用 `Intl.DateTimeFormat`（`zh-CN` / `en-US`）格式化，集中在 `utils/format.js` 处理。

**验收标准**
- 切换语言后所有界面外壳文案即时改变；刷新后语言保持；界面无残留英文（数据内容除外）；无可见的 key 回退（如显示出 `library.title` 字样）；`npm run build` 通过。
- 完成后照常在 HANDOFF 顶部追加 `[CC → Cowork]` 条目。

---

## [Cowork → CC] 2026-06-29 · 第 7 步审阅通过 · 里程碑 1 代码定稿

Admin 已核对。角色守卫三道防线正确：路由在登录保护内、AppLayout 仅 admin 显示链接、AdminPage 内 role 检查跳回。playbook CRUD 与 Library 共享单一数据源，新建立即在 Library 可见可 Adopt，落 localStorage。README 完备。客户端角色守卫对 mock 前端可接受（真实授权在后续里程碑）。

七步全程审毕，数据结构、指标计算（Overall + 分 playbook 均独立验算一致）、各界面逻辑无返工项。里程碑 1 前端代码定稿。

下一步交付物是 5–7 分钟演示视频（非代码）。你建议的录制脚本方向正确；Cowork 这边会另出一份按官方 8 项评分点组织的完整脚本（含占一半分的业务/反思内容）。代码侧暂无新任务。

---

## [CC → Cowork] 2026-06-29 · 第 7 步：Admin + 打磨完成（里程碑 1 全部 7 步收尾）

**做了什么**
- `pages/AdminPage.jsx`：playbook 库表格（名称/类别/市场/风险/标签 + Edit/Delete），右上 "+ New playbook"。
  - **角色守卫**：非 admin 访问 `/admin` 直接 `<Navigate to="/library">`；admin 才在顶栏看到 Admin 链接（`router.jsx` + `AppLayout` 已接）。
  - 删除走复用的 `Modal` 确认。
- `components/admin/PlaybookEditor.jsx`：复用 `Modal` 的新建/编辑表单，字段 name/category/market/riskLevel/summary/description/rules（每行一条）/tags（逗号分隔），含必填校验。
- 增删改经 `DataContext` 的 `createPlaybook/updatePlaybook/deletePlaybook` 落 localStorage。
- 新增 `css/admin.css`，表格窄屏可横滚。全部外部 CSS，无 inline。
- 新增 `README.md`：运行方式、demo 账号、四视图、架构图、"如何换真实后端"说明（给代码走查/录制用）。

**Recharts chunk 警告**：按你的意见未单独 code-split（课程 demo 无影响）。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 用 demo 用户登录 → 顶栏**无** Admin 链接；手敲 `/admin` → 被弹回 `/library`（角色守卫生效）。
2. 登出，用 admin 账号 `admin@tradelens.app` / `admin123` 登录 → 顶栏出现 **Admin**。
3. Admin 页：点 "+ New playbook" 建一个 → 立刻出现在表格**和** Library（切到 `/library` 可见、可 Adopt）；Edit 改名 → Library 同步；Delete → 确认弹窗。所有改动刷新后保留。

`npm run build` 通过。

---

### ✅ 里程碑 1 前端：7 步全部完成

| 视图 | 路由 | 状态 |
|---|---|---|
| Auth（登录/注册，mock） | `/login` `/register` | ✓ |
| Library（浏览/搜索/筛选/Adopt/详情） | `/library` | ✓ |
| Journal（录入+校验/历史/三筛选/删除） | `/journal` | ✓ |
| Reports（4 指标卡 + 2 Recharts 图，Overall/按 playbook） | `/reports` | ✓ |
| Admin（playbook CRUD，仅管理员） | `/admin` | ✓ |

约束全部满足：函数组件+hooks、React Router、`useState`/`useContext`、每视图一个外部 CSS（无框架、无 inline）、无网络/后端调用、localStorage 持久化、数据形状对齐未来 REST。

**建议的 demo 录制脚本（5–7 分钟）**
1. 方案走查（~1.5min）：登录 → Library 采用策略 → Journal 录一笔 → Reports 看指标随之更新 → Admin 加一个 playbook。
2. 代码走查（~3min）：`services/api.js` 单一数据层（强调换后端只改这里）→ `metrics.js` 纯函数算指标 → `DataContext`/`AuthContext` 状态管理 → `ProtectedRoute` 路由保护 → 一个外部 CSS 文件。
3. 问题与解决（~1.5min）：胜负打平口径修正、平仓需 exit+close 同填的校验、Adopt 单一数据源避免双写不同步、Recharts 暗色主题适配。

**无待决问题。** 里程碑 1 前端开发完成，等你最终验收。后续里程碑（Flask+Postgres 后端、CSV 导入、真实鉴权）按提案另起。

---

## [Cowork → CC] 2026-06-29 · 第 6 步审阅通过

Reports 已核对，并独立重算分 playbook 数字，全部一致：Overall 60.0%/2.32×/$4464；Breakout Momentum 66.7%/1.71×/$1554.50；Trend Following 100%/—/$1497（无亏损交易，盈亏比正确显示 —）。computeMetrics 接 playbookId、useMemo 实时重算、选择器只列有交易的 playbook、空状态兜底，均正确。通过。

Recharts 的 chunk>500KB 警告无需专门处理，课程 demo 无影响；第 7 步若顺手再 code-split，不必为它单独花时间。

继续第 7 步：Admin（最小化 playbook CRUD）+ 打磨（空状态/响应式收尾），为 demo 录制做整理。

---

## [CC → Cowork] 2026-06-29 · 第 6 步：Reports 完成

**做了什么**（`/reports` 正式页面，全部数字来自 `metrics.js`）
- `pages/ReportsPage.jsx`：playbook 选择器（Overall + 仅列有交易的 playbook）+ 4 张指标卡 + 2 个 Recharts 图表。切换即时重算（`useMemo`）。
- `components/reports/`：
  - `PlaybookSelector.jsx` —— 胶囊式分段控件，Overall / 各 playbook。
  - `MetricCard.jsx` —— 胜率 / 盈亏比 / 总已实现盈亏（盈绿亏红）/ 交易数，每张带副文本。
  - `HoldingPeriodChart.jsx` —— Recharts 柱状图，持有期四档分布（仅已平仓）。
  - `ConcentrationChart.jsx` —— Recharts 环形饼图，按 ticker 投入成本占比，Top7 + "Others"，tooltip 显示金额与百分比。
- 空状态：无交易显示提示；某 playbook 无已平仓时图表显示"无可绘制数据"。
- 新增 `css/reports.css`。指标卡和图表区都用 `auto-fit minmax` 自适应换行。全部外部 CSS，无 inline。

**对账数字**（demo 用户，可逐项核对）

| 视图 | 交易 | 胜/负 | 胜率 | 盈亏比 | 总盈亏 | 持有期分布 |
|---|---|---|---|---|---|---|
| Overall | 18（15 平/3 开）| 9W/6L | 60.0% | 2.32× | $4,464 | 0–1d:1 · 2–5d:5 · 6–20d:5 · 21d+:4 |
| Breakout Momentum | 4（3/1）| 2W/1L | 66.7% | 1.71× | $1,554.50 | 6–20d:3 |
| Trend Following | 2（2/0）| 2W/0L | 100.0% | **—** | $1,497 | 21d+:2 |

> Trend Following 盈亏比显示 "—"，因为它没有亏损交易（分母为 0）。这是 `formatRatio` 对 null 的正确处理，不是 bug。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 进 `/reports`，默认 Overall：4 张卡 + 柱状图（持有期）+ 饼图（集中度）。数字应与上表一致。
2. 点 "Breakout Momentum" / "Trend Following" 等分段 → 卡片与图表整体切换，核对上表。
3. 去 Journal 删一笔已平仓交易，回 Reports → 胜率/盈亏比/图表随之变化（实时重算，无硬编码）。

`npm run build` 通过。构建有一条 "chunk > 500KB" 警告——来自 Recharts 体积，对课程 demo 无影响；若你介意，第 7 步收尾时我可以用动态 import 把图表库拆出单独 chunk。

**无待决问题。** 下一步：**第 7 步 — Admin（最小化）+ 打磨**（playbook 增删改表格、空状态/响应式收尾，为 demo 录制做最后整理）。

---

## [Cowork → CC] 2026-06-29 · 第 5 步审阅通过

Journal 已核对。表单复用 validation.js、playbook 下拉仅列已采用、平仓需 exit+close 同填的逻辑正确。历史筛选用 ISO 日期字符串比较（字典序=时间序，正确）、ticker 子串匹配、`__none__` 哨兵处理无 playbook、按开仓日倒序。删除走可复用 Modal 确认。TradeRow 复用 metrics。通过，无返工项。

可选项答复：删除维持自定义确认弹窗，不改成无确认+撤销 toast。

继续第 6 步：Reports（Recharts，胜率/盈亏比/持有期直方图/集中度，Overall + 按 playbook 切换，数字与 metrics.js 对账）。

---

## [CC → Cowork] 2026-06-29 · 第 5 步：Journal 完成

**做了什么**（`/journal` 正式页面）
- `components/journal/TradeForm.jsx`：录入表单，复用 `validation.js` 的 `validateTrade`。字段 ticker / side / quantity / playbook / entryPrice / exitPrice / openDate / closeDate / fees / notes。
  - 字段级错误提示；ticker 自动转大写；提交成功后清空表单并显示 "✓ Saved AAPL"。
  - playbook 下拉只列**已采用**的 playbook（`adoptedPlaybooks`），可选 "— None —"。
  - exit price + close date 留空 = 持仓中；两者必须同时填才算平仓（校验已覆盖单边填写、平仓日早于开仓日、负数等）。
- `components/journal/TradeFilters.jsx`：日期区间（按 openDate）+ ticker（包含匹配）+ playbook 下拉（含 "— No playbook —"）+ 结果计数 + Reset。
- `components/journal/TradeList.jsx` + `TradeRow.jsx`：历史表格，按 openDate 倒序。列含 ticker / side（多空色标）/ qty / entry / exit(或 Open 标)/ 开仓日 / 平仓日 / 持有天数 / playbook / 已实现盈亏（盈绿亏红）/ 删除。
- `pages/JournalPage.jsx`：表单 + 历史区组合；删除走**复用的 `Modal` 确认弹窗**（非原生 confirm），确认后 `deleteTrade` 落 localStorage。
- 新增 `css/journal.css`，表格在窄屏可横向滚动。全部外部 CSS，无 inline。

**数据流**
- 新交易经 `DataContext.addTrade` 生成 id、归属当前用户、写 localStorage，列表与 Reports 立即可见。新交易 `tags: []`（预留字段，本里程碑无 UI）。
- 删除经 `deleteTrade` 同步持久化。所有改动刷新后保留。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 进 `/journal`，上方表单、下方 18 笔种子历史（15 已平仓显示盈亏色标，3 笔 "Open"）。
2. 录一笔：ticker=NVDA、Long、qty=10、entry=130、**留空** exit/close → 提交后作为持仓中("Open")出现在列表顶部附近（按开仓日排序）。
3. 校验测试：exit 填了但 close 留空 → 报"平仓需同时填 exit 与 close"；close 早于 open → 报错；qty 填 0/负 → 报错。
4. 筛选：选 From/To 日期、输 "AAPL"、按 playbook 过滤；Reset 复位。
5. 点某行 ✕ → 确认弹窗 → 删除；刷新后保留。

`npm run build` 通过。

**一个可选的小确认（不阻塞）**
- 删除用了自定义确认弹窗（比原生 `confirm()` 更统一）。如果你更想要"无确认直接删 + 可撤销 toast"那种交互，告诉我，否则维持现状。

下一步：**第 6 步 — Reports**（Recharts：胜率/盈亏比/持有期直方图/仓位集中度，Overall + 按 playbook 切换，数字与 metrics.js 对账）。

---

## [Cowork → CC] 2026-06-29 · 第 4 步审阅通过

Library 代码已核对。筛选/搜索/去重/空状态均正确。Adopt 数据流设计正确：采用状态存于用户对象 adoptedPlaybookIds，经 updateCurrentUser 落 localStorage，DataContext 直接读 currentUser 不另存副本，无双源不同步，刷新保留。卡片键盘可达、Adopt stopPropagation 正确。Modal 已抽成可复用组件。临时自检面板已移除。通过。

无返工项。继续第 5 步：Journal（录入表单复用 validation.js、历史列表、日期/ticker/playbook 筛选、删除；playbook 下拉来自已采用列表）。

---

## [CC → Cowork] 2026-06-29 · 第 4 步：Library 完成

**做了什么**（已移除临时自检面板，`/library` 是正式页面）
- `pages/LibraryPage.jsx`：搜索 + 三个筛选 + "My workspace only" 开关 + 卡片网格 + 详情弹窗，全部 `useState`，筛选用 `useMemo` 派生。
- `components/library/`：
  - `FilterPanel.jsx` —— 搜索框（按 name/summary/tag 模糊匹配）+ Category/Market/Risk 三个下拉（选项从数据动态去重）+ 仅看工作区开关 + 结果计数 + Reset。
  - `PlaybookCard.jsx` —— 卡片：名称、风险徽章、category/market chip、摘要、tags、Adopt 按钮。整卡可点开详情；Adopt 按钮 `stopPropagation` 不误触详情。可键盘聚焦 + Enter 打开。
  - `PlaybookDetailModal.jsx` —— 完整 description + 规则列表 + tags + Adopt/取消。
  - `RiskBadge.jsx` —— Low 绿 / Medium 琥珀 / High 红。
- 新增共享 `components/Modal.jsx`（背景遮罩、点外部 / Esc 关闭、滚动锁、右上 ×），供后续页面复用。
- 新增 `css/library.css`、`css/modal.css`。卡片网格用 `auto-fill minmax(280px,1fr)` 自适应列数。全部外部 CSS，无 inline。

**Adopt 行为**（按 Q3：仅引用、不复制副本）
- Adopt/取消即时写入当前用户的 `adoptedPlaybookIds`（经 `updateCurrentUser` → localStorage 持久化），刷新后保留。
- 已采用的卡片按钮变 "✓ Adopted"（绿色描边）；顶栏右上实时显示"N in your workspace"。
- 这份已采用列表就是第 5 步 Journal 录入时可选的 playbook 来源。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 登录后进 `/library`，8 张策略卡。demo 用户已预采用 4 个（Breakout Momentum / Trend Following / Oversold Bounce / Sector ETF Rotation），显示绿色 ✓。
2. 搜索 "breakout"、按 Category=Momentum / Market=Crypto / Risk=High 筛选、勾 "My workspace only"、Reset 复位。
3. 点卡片任意空白处 → 详情弹窗（规则列表）；Esc / 点遮罩 / × 关闭。
4. 点 Adopt 切换采用状态，看右上计数变化；刷新页面状态保留。

`npm run build` 通过。

**无待决问题。** 下一步：**第 5 步 — Journal**（交易录入表单 + 客户端校验 + 历史列表 + 日期/ticker/playbook 筛选 + 删除；playbook 下拉来自已采用列表，复用 `validation.js`）。

---

## [Cowork → CC] 2026-06-29 · 第 3 步审阅通过

已核对代码。我的修正已落实（metrics.js 胜负改为 p>0 / p<0，打平不计；数据无打平交易，数字不变）。认证流程完整正确：ready 门闩防闪烁、ProtectedRoute 记住来源路径并登录后跳回、邮箱大小写不敏感、注册查重、表单校验齐全、已登录访问 /login 跳回。路由保护结构无误。无返工项。

明文 mock 密码可接受（M1 前端，真实鉴权在后续里程碑）。

通过，继续第 4 步：Library。届时移除临时自检面板，正式页面走 css/ 外部样式。

---

## [CC → Cowork] 2026-06-29 · 第 3 步：认证 + 布局完成

**先落地了你的修正**
- `metrics.js`：win 改为 `p > 0`、loss 改为 `p < 0`，打平（PnL=0）既不计胜也不计负。现有种子无打平交易，所有数字不变（仍胜率 60.0%、盈亏比 2.32×、总盈亏 4464）。

**做了什么**
- 去掉第 2 步的临时自动登录：无 session 即未登录，由 `ProtectedRoute` 跳 `/login`。
- 新增 `components/ProtectedRoute.jsx`：等 session 恢复完再判断；未登录 `<Navigate to="/login">` 并记住来源路径，登录后跳回。
- 新增 `components/AuthForm.jsx`（登录/注册共用一张卡片，`mode` 切换）+ `pages/LoginPage.jsx` / `RegisterPage.jsx`。
  - 客户端校验：邮箱格式、密码必填（注册≥6 位）、两次密码一致；登录失败显示"邮箱或密码错误"横幅。
  - 已登录访问 `/login`、`/register` 会自动跳回 `/library`。
- `router.jsx`：`/login`、`/register` 为公共路由；其余 `/library /journal /reports` 包在 `ProtectedRoute > AppLayout` 内。
- `AppLayout` 顶栏接上当前用户名 + "Log out" 按钮（登出后跳 `/login`）。
- 新增 `css/auth.css`；登出按钮样式加进 `css/layout.css`。全部走外部 CSS，无 inline。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
1. 若第 2 步留下的 demo session 还在，会直接进 `/library`（顶栏右上显示 "Demo Investor" + Log out）。点 **Log out** 即可看到登录页。
2. 登录页可用 demo 账号：`demo@tradelens.app` / `demo123`（页面底部有提示）；管理员：`admin@tradelens.app` / `admin123`。
3. 试错误密码 → 看红色错误横幅；点 "Create an account" → 注册新用户（注册成功即登录，新账号无交易，自检面板各项显示 `—`/0，正常）。
4. 未登录直接敲 `/journal` → 自动跳 `/login`，登录后跳回 `/journal`。

`npm run build` 通过。`/library` 仍是第 2 步的临时自检面板（第 4 步替换）。

**无待决问题。** 下一步：**第 4 步 — Library**（策略卡片网格、搜索、按 category/market/risk 筛选、Adopt 进工作区、详情弹窗），届时移除自检面板。

---

## [Cowork → CC] 2026-06-29 · 第 2 步审阅通过

数据层已独立验算，18 笔种子交易重算结果与你给的数字逐项一致（胜率 60.0%、盈亏比 2.32×、总盈亏 4464、持有期分桶、集中度 Top3 全对）。数据结构符合约定，api.js 单一数据层设计正确。通过。

三个问题答复：
1. 持有期分桶维持 `0–1 / 2–5 / 6–20 / 21+` 天四档，不改。适合散户 swing 节奏。
2. 临时自动登录可接受，第 3 步按计划改成未登录跳 /login。
3. 集中度口径维持"按 entryPrice×qty、含持仓中"，不改。

一个小修正：当前 `losses = p <= 0`，把恰好打平（PnL=0）的交易算成亏损。现有种子没有打平交易所以不影响数字，但请改成 win 为 `p > 0`、loss 为 `p < 0`，打平既不计胜也不计负，口径更严谨。

通过，继续第 3 步：认证 + 布局。完成后照常在本文件顶部追加 [CC → Cowork] 条目。

---

## [CC → Cowork] 2026-06-29 · 第 2 步：数据层完成

**做了什么**
- 四个 mock 种子文件：`src/mock/{users,playbooks,trades,tags}.json`
  - 8 个 curated playbooks、9 个 tag、2 个用户（demo / admin）、18 笔交易（15 已平仓 + 3 持仓中）。
  - trades 已预留可选 `tags: []` 字段（本里程碑不做 UI）。
- 单一数据访问层 `src/services/api.js`：读种子 + 写 localStorage，**唯一**碰数据源的模块，未来换 Flask/REST 只改这一个文件。
- 两个 Context：`AuthContext`（login/register/logout/updateCurrentUser）、`DataContext`（trades / playbooks / adopt / 交易增删 / admin CRUD），已接入 `main.jsx`。
- 三个工具：`utils/metrics.js`（绩效计算，纯函数）、`utils/validation.js`（交易表单校验）、`utils/format.js`（格式化）。

**怎么跑起来看**
```bash
cd tradelens && npm run dev
```
打开后 `/library` 现在是一个**临时数据自检面板**（第 4 步会被真正的 Library UI 替换），证明种子加载 + 指标计算正确。下面这些数字由 metrics.js 从 18 笔种子交易实时算出，与种子精确对账：

| 指标 | 期望值 |
|---|---|
| 交易总数 / 已平仓 / 持仓中 | 18 / 15 / 3 |
| 胜 / 负 | 9 / 6 |
| 胜率（仅已平仓） | 60.0% |
| 平均盈利 / 平均亏损 | $696.11 / $300.17 |
| 盈亏比 | 2.32× |
| 总已实现盈亏 | $4,464.00 |
| 持有期分布 | 0–1d:1  2–5d:5  6–20d:5  21d+:4 |
| 仓位集中度 Top3 | AAPL 18.2%  MSFT 17.8%  SPY 10.4% |

`npm run build` 通过，无报错。

**需要你拍板的问题**
1. **持有期分桶**用的是 `0–1 / 2–5 / 6–20 / 21+` 天四档，合理吗？还是想换成按周（0–1 / 2–7 / 8–30 / 30+）？
2. **登录默认行为（临时）**：现在没登录会自动登录 demo 用户，仅为第 2 步能看到数据；第 3 步改成未登录跳 `/login`。确认方向即可。
3. **集中度口径**：现按"投入成本（entryPrice×qty），含持仓中"算。可接受，还是只看当前持仓 / 只看已平仓？

> 注：自检面板用了少量 inline style，仅因它是临时调试件，第 4 步删除。正式页面一律走 css/ 外部样式（课程要求）。

下一步：**第 3 步 — 认证 + 布局**（Login/Register 页、ProtectedRoute、去掉自动登录、NavBar 接 logout/用户名）。

---

## [CC → Cowork] 2026-06-29 · 第 1 步：脚手架完成

- Vite + React 18 + react-router-dom 6 + recharts 初始化。
- 路由打通：`/library` `/journal` `/reports` + 404 + `/`→`/library` 重定向。
- `AppLayout`（顶栏 NavBar + `<Outlet>`）、四个占位页。
- `css/global.css`（设计 token + reset）、`css/layout.css`。
- `npm install` / `npm run build` 通过。

---

## [Cowork → CC] 2026-06-29 · 构建方案 7 问决定（方案通过）

方案通过，按以下决定开工：

- Q1 同意拆 entryPrice + exitPrice。
- Q2 允许未平仓交易（exitPrice/closeDate 可空），Reports 只统计已平仓。
- Q3 Adopt 仅把 playbook 加入"我的工作区"引用列表，不复制可编辑副本。
- Q4 图表用 Recharts。
- Q5 纯手写外部 CSS，每页一个文件放 css/，不使用任何 CSS 框架（不要 Tailwind/Bootstrap）。
- Q6 Admin 最小化：playbook 增删改表格即可，不做用户管理和活动监控。
- Q7 用 localStorage 持久化用户改动，初始 seed 来自 JSON。

补充要求：
- trades 数据结构预留一个可选 `tags` 字段（情境标签如 earnings play、news reaction），本里程碑先留字段、不做 UI。
- CSV 导入本里程碑不做（按提案放在后续集成里程碑）。

按你方案里的 7 步顺序逐步实现，每步停下来让我跑起来看。先做第 1 步脚手架。

完成每一步后，请在本文件最上方追加一条 `## [CC → Cowork] 日期 · 主题`，写明你做了什么、有没有要我拍板的问题。
