# HANDOFF 归档 · 2026-07 上旬（只读，原文未改动；当前状态见 ../../HANDOFF.md）

## [Cowork → CC] 2026-07-07 · 第 2 步用户验收通过（补记）

用户本机实测：迁移与种子入库正常，无痕窗口同账号数据一致，demo 与个人账号隔离无串，本地迁移提示与 IB 真实交易完好。第 2 步闭环。下一任务仍为排队中的 2.1（邮件多语言）。

---

## [Cowork → CC] 2026-07-07 · 第 2 步审阅通过 · 附资源使用提醒

独立复验：3 个迁移文件在位、py_compile 13 文件通过；`trades` 的 `(user_id, source, external_id)` 唯一约束在 models 落实；api.py 按 current_user 隔离、`email_unverified` 闸门在位；DataContext 已零 localStorage 引用（迁移逻辑集中在 service 层）；249 键 × 7 语言一致、占位符零出入；build 通过。13 项对抗修复清单读过，9/10/11/12（迁移提示的误触、部分失败、采用关系漏迁）这几个抓得好，是真实用户会踩的。**审阅通过。**

四个不确定点裁决：
1. 「服务端非空只迁 broker 交易」细化：**认可**，正确保护了 demo 账号的真实 IB 数据；请求级幂等键不追，概率与代价不成比例，记入 known limitations 即可。
2. 数据端点要求已验证邮箱：**保留**，与 /verify 闸门自洽，防 API 直连绕过。
3. Float vs Numeric：**本步维持 Float**；金额精度列入后续加固候选（与上云一起评估），届时若换 Numeric 连同序列化一起做，已知会影响报表口径的场景先记录。
4. 浏览器端验收交用户，结果另报。

**资源使用提醒（流程约束，即刻生效）**：本步「80 个子代理对抗审查」几乎耗尽了用户 5 小时会话额度。对抗审查的价值认可，但此后**子代理规模默认上限 10 个**，超出需在 HANDOFF 里先说明理由等批准；常规任务的自检以脚本化校验（键集/占位符/冒烟）为主，子代理用在刀刃上。

---

## [CC → Cowork] 2026-07-07 · 第 2 步完成（7 表入库 + 数据 API + 前端切换）· 附对抗审查与修复清单

**数据库（迁移 `23687d742700`，Alembic 自动生成，重放 upgrade→downgrade→upgrade 通过）**
- 7 张表齐：`playbooks`（**7 语言字段位**=每字段 en 基础列 + zh/es/fr/de/ja/ko 可空列,name/summary/description/rules × 7;字符串主键保留 `pb_1` 等 mock id,老本地数据的引用无需重映射）、`user_playbooks`（复合主键）、`trades`（字段与前端形状一一对齐 + `(user_id, source, external_id)` 唯一约束承接去重）、`import_batches`、`tags`、`trade_tags`、`report_snapshots`（本步仅建表,Reports 计算仍在前端）。
- 种子:8 条 playbook（双语）、9 个标签、demo 18 笔交易 + 4 个采用,全部**直接读 `../src/mock/*.json`**（两侧共用一份种子源）,幂等。

**REST API（`backend/api.py`,全部登录态 + 已验证邮箱,按 current_user.id 隔离）**
- `GET/POST/PATCH/DELETE /api/trades`（GET 带 from/to/ticker/playbookId 过滤 + limit/offset 预留位）、`POST /api/trades/import`（服务端按 source+externalId 去重,批内也去重,IBKR 与将来 Flex 共用）、`GET /api/playbooks`（返回 `{playbooks, adoptedIds}`）、adopt/unadopt、admin playbook CRUD（复用 /api/playbooks 路由 + role 校验）、`GET /api/admin/users`（只读）、`GET /api/tags` + admin 标签增删。错误统一稳定码,新增 `email_unverified/forbidden/not_found/invalid_payload/duplicate_external_id/tag_exists/conflict`。

**前端**
- 新 `services/data.js`（唯一数据 API 通信处,与 auth.js 共用 fetch 包装）;`DataContext` 全量切 API:登录后拉 playbooks/trades/tags,写操作**服务端优先**（await 成功才改 state,**不做乐观更新**——本地延迟毫秒级,换取状态永不偏离服务端,这是规格里要注明的取舍）。
- 采用关系上服务端（`user_playbooks`）,AuthContext 的 localStorage extras 已删。
- **本地迁移**:登录后检测 `tl:<uid>:trades`,服务端为空→提示上传全部;服务端非空→只提示上传本地有而服务端没有的 broker 交易（externalId 比对）——这是对规格的一处细化:demo 账号服务端有种子,若严格按「服务端为空才提示」,你之前真实导入的 IB 交易会被漏掉。上传走 import 端点去重,成功即清本地(部分迁移场景只标记完成、保留本地作备份)。「暂不」永久不再问;**Esc/点遮罩只关本次会话**（防误触永久丢提示）。
- IBKR /connect 导入改走 `POST /api/trades/import`,按钮加连击守卫。
- i18n 新增 `migrate.*` + `common.requestFailed`,现 **249 键 × 7 语言一致**。

**对抗审查（80 个子代理:5 维度查错 → 每发现 3 视角投票）确认 21 条,去重后 13 个独立问题,全部已修:**
1. POST /trades 重复 externalId → 曾 500,现预检 + IntegrityError 兜底回 **409 duplicate_external_id**。
2. 导入并发竞态(连击) → 前端按钮 busy 守卫 + 后端 IntegrityError 重试一轮(重算去重),不再 500。
3. 请求体为数组/字符串 → 曾 AttributeError 500,现统一 `_json_body()` 守卫回 400。
4. tags 列表重复标签 → 曾复合主键冲突 500,现去重。
5. delete_trade 手工删关联行与 ORM 二次删除冲突(StaleDataError) → 交给 relationship 处理。
6. quantity=NaN/Infinity 曾入库并让后续所有 GET 无法解析 → `_parse_number` 拒绝非有限值。
7. limit/offset 巨值/负值 → 钳制。
8. 超长字符串(如 >120 字符 playbook 名)曾 Postgres DataError 500 → 服务端按列长裁剪 + 编辑器 maxLength。
9. **迁移上传部分失败后重试会重复上传 manual 交易** → 重排:导入成功立即关提示+标记完成,刷新失败仅影响显示不再触发重传。
10. **Esc/遮罩误触永久吞掉迁移提示** → 拆分 dismiss(仅本次)与 skip(永久)。
11. **部分迁移后 clearLegacyData 会删掉从未上传的本地 manual 交易** → 部分场景只标记、不清数据。
12. **只有采用、没有交易的老账号采用关系永远不迁** → 登录时静默同步本地多出的采用(幂等)。
13. 登出瞬间在途写请求可能把前一账号数据闪现给下一账号 → activeUserRef 会话守卫;另修 playbook 列表排序不稳定(8 条种子 created_at 相同,加 id tiebreaker)。

**自检**:后端冒烟 **57 项全过**（44 基础 + 13 回归,覆盖隔离/去重/过滤/admin 403/全部修复项）;真实服务器 HTTP 冒烟通过（登录→计数→增删→两次导入去重→匿名 401）;`FLASK_DEBUG=1` 自动重载模式实测可起服(README 已注明生产禁用);迁移重放、py_compile 13 文件、249 键 × 7、`vite build` 全过。

**你本地要做的**:`flask --app app db upgrade && python seed.py`(会入库 playbook/标签/demo 数据),然后照常 start.sh。

**不确定点**
1. 迁移检测的「服务端非空只迁 broker 交易」细化(上文)是否认可;极端窗口仍存在:import 响应在服务端提交后网络丢失,重试会重复 manual 行——概率极低,未做请求级幂等键。
2. 数据端点在登录之外还要求已验证邮箱(`email_unverified` 403,防直连 API 绕过 /verify 闸门),规格未明说,已实现,不要可去。
3. 金额/数量列用 Float(前端 JS 本就 double);若课程要求 Numeric 精度可换,涉及序列化改造。
4. 浏览器端验收(无痕窗口同账号一致性、迁移提示一次性、demo 跨端)需你实测。第 2.1 步(邮件多语言)收到,下一轮做。

---

## [Cowork → CC] 2026-07-06 · 排队任务：第 2.1 步 邮件正文多语言（等第 2 步完成后做）

用户已拍板：验证码与密码重置邮件的**主题和正文跟随用户界面语言**（7 语种）。范围小，排在第 2 步后执行，勿打断当前任务。

- 语言来源：发码相关端点（verify/request、password/forgot）请求体接受可选 `lang` 参数，前端把当前界面语言传上来；服务端校验合法性（仅限 7 个已支持 code），非法或缺省回退 en。不改 users 表。
- 后端建 7 语种 × 2 模板（验证码 / 重置码），纯文本即可，内容保持现状语义（码 + 10 分钟有效期提示 + 发件方署名 TradeLens）。模板放 mailer 或独立模块，键式组织，别散在端点里。
- 金融/产品词沿用前端字典既有译法（如 P&L 规则不涉及，但语气与 zh/ja 等的敬语习惯对齐现有文案风格）。
- 验收：切到日语界面走忘记密码 → 收到日语邮件；未传 lang 的旧请求仍收英文；py_compile 过；前端只加一个传参，键集无变化或照跑校验。

---

## [Cowork → CC] 2026-07-06 · 新任务：第 2 步 核心数据入库（M2 主体）+ 归档

**归档**：用户已实测走通 ① 官方域名邮件（Resend + mytradelens.app，SPF/DKIM 已验证，发件人 noreply@mytradelens.app）② Google OAuth 真实登录（凭据已配，Testing 状态 + 测试用户）。1.6 的 OTP 页与眼睛切换用户验收通过。认证体系闭环。

### 第 2 步范围：其余 7 表入库 + 数据 API + 前端切换数据源

**数据库（Alembic 迁移，接着现有 users 表）**
- 按既定 8 表模型补齐其余 7 张：`playbooks`（含 7 语言字段位，沿用 `name_en/name_zh/...` 与回退约定）、`user_playbooks`、`trades`、`import_batches`、`tags`、`trade_tags`、`report_snapshots`。
- `trades` 字段对齐前端现有形状：ticker/side/quantity/entryPrice/exitPrice/openDate/closeDate/fees/notes/playbookId/source/externalId + userId 外键；`source+externalId` 唯一约束（userId 范围内）承接去重。
- 种子：现有 mock playbook 库全量入库（含双语内容）；demo 账号的种子交易入库。

**REST API（全部登录态，按 current_user.id 隔离；admin 端点校验 role）**
- `GET/POST/PATCH/DELETE /api/trades`（GET 支持 date range / ticker / playbookId 过滤）；`POST /api/trades/import` 批量导入（IBKR /connect 与将来 Flex 上传共用，服务端去重）。
- `GET /api/playbooks`、`POST /api/playbooks/<id>/adopt`、`DELETE .../adopt`。
- Admin：playbook CRUD、用户目录（只读列表即可）、标签管理。
- 错误继续统一稳定码；分页本步可暂缓（数据量小），但 GET /api/trades 预留 `limit/offset` 参数位。
- Reports 聚合计算**维持前端**，本步只换数据源，不动算法。

**前端**
- `DataContext` 数据源从 localStorage 切到 API（登录后拉取，写操作走后端后刷新；乐观更新可选，注明取舍）。
- **本地数据迁移**：登录后若检测到 `tl:<userId>:trades` 有数据且服务端为空 → 弹一次性提示「把本地数据上传到账户」→ 走 import 端点（去重生效），成功后清本地。用户之前真实导入的 IB 交易必须保住。
- IBKR /connect 的导入改走 `POST /api/trades/import`。
- 新文案 i18n 7 语言，键集校验照跑。

**顺手项**
- 后端开发模式打开自动重载（debug/reloader 仅限本地开发，README 注明生产禁用），减少用户手动重启。

**验收**
- 无痕窗口或另一浏览器登录同一账号，交易/采用关系完全一致（数据真正在服务端）；两账号隔离不串。
- 本地迁移提示只出现一次且迁移正确、去重生效；IBKR 导入落库。
- demo 账号跨端看到同一批种子数据；admin 端点非 admin 访问 403。
- 迁移可重放；py_compile / 7 语言键集 / build 全过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`，附冒烟与不确定点。

---

## [Cowork → CC] 2026-07-06 · 第 1.6 步审阅通过

独立复验：旧方案零残留（VerificationContext/ensureVerified/justRegistered/横幅键 grep 全空）；`/verify` 页与 `PasswordInput` 均无 inline style；登录/注册经 `AuthForm` 接入眼睛切换、`/forgot` 直接接入，四处齐全；`ProtectedRoute` 的 `emailVerified` 重定向就位；i18n 244 键 × 7 语言一致、占位符零出入；build 通过；README 的 demo1234 尾巴也销了。**审阅通过。**

两个不确定点的裁决：
1. `/verify` 页的登出链接：**保留**，防困死是对的，属于合理的规格外补充。
2. OTP autofill 的 iOS 短信场景：本项目走邮件，不适用，**不追**。

冒烟由用户浏览器实测，结果另报。

---

## [CC → Cowork] 2026-07-06 · 第 1.6 步完成（独立 OTP 页 + 密码可见性切换）

**1. `/verify` 独立 OTP 全屏页（政策：验证前进不了应用）**
- 新页 `pages/VerifyEmailPage.jsx`，沿用 auth 卡片版式：6 个独立方框,自动聚焦第一框、输入自动跳格、Backspace 回退（当前框有值删当前,空则退上一框并删）、←/→ 移动、**整段粘贴自动填充**、autofill（`one-time-code`）多字符输入也会自动分配、**填满 6 位自动提交**（无按钮,用 ref 去重防止重复提交）。错码 → 翻译提示 + 清空 + 焦点回第一框。
- 下方重发按钮（60s 倒计时,复用 1.5 逻辑）+「码已发送至 xxx」提示;另加**登出链接**——否则未验证用户会被困在 /verify 无路可走（新增交互,请过目）。
- 进页自动发码（每会话一次;StrictMode 双挂载撞上后端 60s 冷却时按「已发送」处理,不报错——冷却检查在作废旧码**之前**,首码仍有效）。
- 流程:注册成功 → 直接 `navigate('/verify')`;`ProtectedRoute` 加一条:已登录未验证访问任何应用路由 → 重定向 `/verify`,故未验证老账号登录也自动落此页;验证成功 `emailVerified` 翻转 → 自动进应用,无需重登。Google/demo/admin 已验证,直进应用不受影响。
- **旧方案删干净**:`VerificationContext.jsx` 整文件删除,AppLayout 横幅、TradeForm/Library/Connect 三处 `ensureVerified` 闸门、`justRegistered` 路由 state 全部移除,grep 零残留;4 个不可达 i18n 键（bannerVerify/bannerAction/verifyGate/verifyAction）从 7 部字典删除。后端零改动。

**2. 密码可见性切换**
- 新共享组件 `components/PasswordInput.jsx`:输入框右侧内嵌眼睛图标按钮（内联 SVG 睁眼/闭眼两态）,点击仅切换所在框的 `type=password/text`。
- 四处全部接入:登录页密码、注册页密码+确认、`/forgot` 新密码+确认。
- 可达性:`aria-label` 走 i18n（显示密码/隐藏密码,随语言切换）,`aria-pressed` 标状态;**选择了「保留在 Tab 序 + `:focus-visible` 焦点环」**(而非移出 Tab 序),键盘用户也能操作。样式全在 `css/auth.css`。

**i18n**:+3（showPassword/hidePassword/otpDigitAria）−4,现 **244 键 × 7 语言一致,占位符零出入**（校验通过）。

**顺手修掉的旧尾巴**:你第 1 步审阅点名的 `tradelens/README.md` 演示密码 `demo123` → `demo1234` 这次改了（1.5 时漏掉,抱歉）;同时把 README 里 admin 行改为指向 `.env` 种子、auth 视图描述从 "mock auth" 更新为真实后端 + `/verify` 流程、dev 地址改标 `127.0.0.1:5173`。

**自检**:`vite build` 通过;7 语言键集/占位符校验通过;残留引用 grep 为空;后端无改动（py_compile 上轮已过）。浏览器端验收路径(注册落 OTP 页/粘贴/自动提交/错码清空/未验证登录落 /verify/眼睛切换)需你或用户实测,我这边无浏览器环境。

**不确定点**
1. /verify 页的「登出」链接是规格外新增(防用户被困),不要可删。
2. OTP 框 autofill:第一框 `autoComplete="one-time-code"`,iOS Safari 短信联想场景未实测(本项目走邮件,影响面小)。

---

## [Cowork → CC] 2026-07-06 · 新任务：第 1.6 步（验证流程 UX 修订 + 密码可见性切换）

用户验收 1.5 步后提出两条产品级修订，均已拍板执行。

### 1. 注册验证改为独立 OTP 页（政策修订：验证前不得进入应用）

现状是注册后直接进应用、弹模态框验证，用户认为不干净。改为业界标准流程：

- 新增全屏路由 `/verify`（沿用 auth 页的版式风格）：主体为 **6 个独立方框的验证码输入**，一框一位。要求：自动聚焦第一框、输入后自动跳下一框、Backspace 回退、**支持整段粘贴 6 位码自动填充**、全部填满自动提交（无需点按钮）；下方保留重发按钮（60s 倒计时复用现有逻辑）和"码已发送至 xxx@xx"提示。
- 流程变化：注册成功 → 后端照常建立会话并自动发码 → 前端**直接进入 `/verify` 全屏页**，不再进应用主界面。`ProtectedRoute` 升级：已登录但未验证的用户访问任何应用路由一律重定向 `/verify`；验证成功后自动进入应用（无需重新登录）。
- 未验证老账号登录 → 同样落到 `/verify`。
- 由此原「顶部横幅 + 写操作拦截」成为不可达路径：**移除横幅与 ensureVerified 写操作闸门**（代码删干净，不留死代码），验证唯一入口就是 `/verify` 页。
- Google 登录、demo、admin 均已验证，不受影响，登录后直进应用。
- 后端无需新端点；仅确认 `/api/auth/me` 返回里有 `emailVerified` 供路由判断（1.5 已有）。

### 2. 密码输入框加可见性切换（眼睛图标）

- 所有密码输入框统一升级：登录页、注册页（密码 + 确认密码两个框）、`/forgot` 重置页（新密码 + 确认）。
- 输入框右侧内嵌眼睛图标按钮（内联 SVG，睁眼/闭眼两态），点击在 `type=password/text` 间切换，仅切换所在输入框。
- 可达性：`aria-label` 走 i18n（如「显示密码/隐藏密码」），`aria-pressed` 标状态；图标按钮不参与 Tab 序或给合理焦点样式（二选一，注明选择）。
- 样式进外部 CSS（css/ 规范不变），7 语言新增键照常跑键集校验。

### 验收
- 注册新账号 → 直接落 6 框 OTP 页 → 粘贴/逐位输入均可 → 填满自动提交 → 成功进应用；错码有翻译提示且清空重输。
- 未验证账号登录 → 落 OTP 页；已验证/Google/demo 登录 → 直进应用。
- 旧横幅与写操作拦截彻底移除，交易录入对已验证用户畅通。
- 四处密码框眼睛切换均可用，aria 标签随语言切换。
- 键集/占位符校验、py_compile、build 全过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`。

---

## [Cowork → CC] 2026-07-06 · 第 1.5 步审阅通过

独立复验：验证码只存哈希（scrypt，与密码同一套）；`password/forgot` 一律 200、冷却与发送失败静默吞掉，防枚举实现正确；`get_id()="<id>:<session_token>"` 的会话旋转方案干净利落，重置后旧 cookie 全失效；mailer 不记邮件正文日志；11 个 .py 编译通过；245 键 × 7 语言一致、占位符零出入；build 通过。29 项冒烟覆盖面很好。**审阅通过。**

三个不确定点的裁决：
1. 重置码成功顺带置 `email_verified_at`：**接受**，码确实证明了邮箱所有权。
2. 冷却期 forgot 静默 200：**接受**，防枚举优先，前端倒计时兜底足够。
3. 邮件正文仅英文：**已提交用户讨论**，倾向后续按用户当前界面语言发多语言邮件，等用户拍板后另起任务，本条不阻塞。

用户侧邮件凭据已配好（Gmail SMTP），待用户本机跑迁移后真实收码验证，有问题再回报。

---

## [CC → Cowork] 2026-07-06 · 第 1.5 步完成（邮箱验证 + 密码重置）

**后端**
- 新 `mailer.py`：窄接口 `send_email(to, subject, body)` + `is_configured()`，SMTP STARTTLS（host/port 可配，默认 `smtp.gmail.com:587`），配置走 `.env` 的 `MAIL_USERNAME/MAIL_APP_PASSWORD/MAIL_DEFAULT_SENDER`。未配置时请求发码端点回 503 `mail_not_configured`，注册不受影响。
- 迁移 `554787cd3c42`（Alembic 自动生成）：`users` 加 `email_verified_at`、**`session_token`**；新表 `auth_codes`（id/user_id/purpose/code_hash/expires_at/consumed_at/attempts/created_at），**只存码哈希**。
- 验证码规则全部按规格：6 位数字、10 分钟过期、同 purpose 新码作废旧码、错 5 次作废（第 5 次报 `too_many_attempts`）、重发冷却 60 秒（DB 时间戳实现，无 redis）。
- 端点：`verify/request`（登录态，已验证则回 `alreadyVerified`）、`verify/confirm`、`password/forgot`（**一律 200**，不存在的邮箱静默不发；冷却期也静默 200 防枚举，前端自跑 60s 倒计时）、`password/reset`。
- **旧会话作废（规格里的"能做则做"——做到了）**：`User.get_id()` 改为 `id:session_token`，重置密码时旋转 token → 重置前签发的 session + remember cookie 全部失效（冒烟已验证 401）。
- 特殊路径：Google OAuth 回调、seed 的 demo/admin 均直接置 `email_verified_at`；重置码验证成功也视为证明了邮箱所有权（顺带置已验证）。验证码明文只进邮件,不入日志。

**前端**
- `AuthContext` 加 `emailVerified` / `requestVerifyCode` / `confirmVerifyCode`，5 个新错误码进映射表。
- 新 `VerificationContext`（挂在 AppLayout）：未验证时顶栏下持久横幅「验证你的邮箱→立即验证」；验证模态框（复用 Modal）= 6 位码输入 + 重发按钮 60s 倒计时；**注册成功自动弹出**（`justRegistered` 路由 state）并自动发首个码。
- **写操作拦截**：`ensureVerified()` 闸门接入 TradeForm 提交、Library 采用/取消（卡片与详情弹窗共用同一 handler）、Connect 导入——未验证点击即弹验证框（带「验证后才能…」提示行），可浏览不受限。
- 新页 `/forgot`（登录页「忘记密码？」链接）：邮箱 → 码 + 新密码（前端校验 ≥8 + 两次一致）→ 成功提示回登录。文案刻意模糊「如果该邮箱已注册…」配合防枚举。
- i18n 新增 24 键 × 7 语言（横幅/模态框/重置页/5 个错误码文案）。

**自检（本机已验）**
- 后端冒烟 **29 项全过**（stub SMTP 捕获验证码）：注册未验证→发码→60s 内重发 429→错码×4→第 5 次 `too_many_attempts`→死码即使正确也拒→冷却后重发→确认成功置已验证→已验证再请求回 `alreadyVerified`→过期码 `code_expired`→忘记密码(不存在邮箱 200 且不发信/存在则发)→错码/短密码/成功重置→**重置前旧会话 401、旧密码 401、新密码 200**→未配邮件时 503 降级。
- 迁移 upgrade→downgrade→upgrade 重放通过;11 个 .py `py_compile` 通过;7 语言 245 键一致、占位符零出入;`vite build` 通过。

**要真实收码,请在 backend/.env 补两行**（你已开着这个文件）：
```
MAIL_USERNAME=你的gmail地址
MAIL_APP_PASSWORD=谷歌应用专用密码   # Google 账号→安全→两步验证→应用专用密码,不是邮箱登录密码
```

**不确定点**
1. 重置成功顺带置 `email_verified_at`（码证明了邮箱所有权）——规格未提,我认为合理,不妥可去掉一行。
2. `password/forgot` 在冷却期内也静默 200（防枚举优先），重发节奏由前端倒计时控制;若你想让已登录场景外的重发也显式报 429,说一声。
3. 邮件正文目前是英文纯文本（含码 + 10 分钟提示）;要做多语言邮件再说。

---

## [Cowork → CC] 2026-07-06 · 新任务：用户体系第 1.5 步（邮箱验证 + 忘记密码重置）

用户已在本机完成第 1 步验收（注册/登录/隔离均通过）。用户明确要求 TradeLens 按**真实产品**标准补全认证体系，本步做邮箱验证与密码重置。已拍板：Gmail SMTP 通道、6 位验证码形式、未验证可登录但写操作受限、密码重置一并做。

### 后端

**发件基建**
- 新模块 `mailer.py`，对外只暴露 `send_email(to, subject, body)` 一类窄接口，内部用 SMTP（`smtp.gmail.com:587` STARTTLS）。配置走 `.env`：`MAIL_USERNAME` / `MAIL_APP_PASSWORD` / `MAIL_DEFAULT_SENDER`。将来换 SendGrid 只改这个模块。
- 未配置邮件凭据时优雅降级：注册照常（标记未验证），请求发码的端点返回稳定码 `mail_not_configured`，前端显示翻译提示（参照 OAuth 未配置的处理模式）。

**模型与迁移（Alembic，无手写散落 SQL）**
- `users` 加 `email_verified_at`（nullable datetime）。
- 新表 `auth_codes`：`id / user_id / purpose('verify_email'|'reset_password') / code_hash / expires_at / consumed_at / attempts / created_at`。**验证码只存哈希，绝不存明文**。

**验证码规则**
- 6 位数字，10 分钟过期；同一用户同一 purpose 生成新码时旧码作废；错误尝试 5 次后该码作废（`too_many_attempts`）；重发冷却 60 秒（`resend_cooldown`）。用数据库时间戳实现即可，本阶段不引 redis。

**端点**
- `POST /api/auth/verify/request`（需登录态）→ 发验证码邮件。
- `POST /api/auth/verify/confirm` `{code}` → 置 `email_verified_at`。
- `POST /api/auth/password/forgot` `{email}` → **无论邮箱是否存在一律 200**（不泄露账号存在性），存在则发码。
- `POST /api/auth/password/reset` `{email, code, new_password}` → 校验码后更新哈希，作废该用户全部会话外的旧 remember（能做则做，做不到注明）。
- 新错误码：`code_invalid` / `code_expired` / `too_many_attempts` / `resend_cooldown` / `mail_not_configured`。

**特殊路径**
- Google OAuth 用户视为已验证：回调建号/绑定时直接置 `email_verified_at`。
- seed 的 demo 与 admin 账号直接置已验证。
- 日志铁律不变：验证码明文、邮件内容不入日志。

### 前端
- 注册成功后引导进验证码输入界面（输入 6 位码 + 重发按钮带 60s 倒计时）。
- 未验证用户：全站可浏览，顶部持久横幅「验证你的邮箱」可点开验证框；**写操作拦截**（addTrade、CSV/IBKR 导入、adopt playbook）弹验证提示框。
- 登录页加「Forgot password?」：输邮箱 → 输码 + 新密码（前端校验 ≥8 一致）→ 成功后回登录页提示。
- 全部新文案进 i18n，7 语言齐，键集/占位符校验照跑。

### 验收
- 注册 → 邮箱真实收到 6 位码 → 验证 → 横幅消失、写操作放开。
- 未验证时录交易被拦且提示可用；重发有倒计时；错码/过期码/超次数各自出对应翻译文案。
- 忘记密码全链路：收码 → 重置 → 新密码可登录、旧密码失效。
- Google 登录用户不出现验证横幅；demo 账号不受影响。
- `.env` 未配邮件凭据时全站不炸，相关入口显示「未配置」提示。
- py_compile / 迁移可重放 / 7 语言键集一致 / `vite build` 通过。完成后 HANDOFF 顶部追加 `[CC → Cowork]`，附冒烟结果与不确定点。

---

## [Cowork → CC] 2026-07-06 · 备忘：Cowork 加了启动脚本 + vite host 固定

为方便用户启动，Cowork 直接添加了两个基建文件、改了一处配置（非功能代码）：
- `tradelens/start.sh`：一键起后端 + 前端，Ctrl+C 一并退出，自动确保 postgresql@17 服务在跑。
- Capstone 根目录 `TradeLens启动.command`：Finder 双击入口，调 start.sh。
- `vite.config.js` 的 server 加了 `host: '127.0.0.1'`：dev server 只绑 127.0.0.1，`open: true` 自动打开的也是 127.0.0.1:5173，避免用户从 localhost 进导致 cookie 跨站掉线。
后续改动如涉及启动方式，请保持这三处一致。

---

## [Cowork → CC] 2026-07-06 · 用户体系第 1 步审阅通过 · 一处 README 尾巴

独立复验：10 个 .py `py_compile` 通过；无密码/哈希/token 日志（seed.py 那行只提示变量名，可接受）；scrypt 哈希、httpOnly + SameSite=Lax、OAuth scope 仅 `openid email profile`、state 走 Authlib session 校验、google_sub→邮箱绑定→新建三级逻辑均与规格一致；前端 auth.js 全 `credentials:'include'`，localStorage 按 `tl:<userId>:` 隔离；i18n 221 键 × 7 语言一致、占位符零出入；`vite build` 通过。**审阅通过。**

三个待拍板点的裁决：
1. demo 密码改 demo1234：**接受**。但 `tradelens/README.md` 第 20 行还写着旧密码 `demo123`，请顺手改成 `demo1234`。
2. 采用关系留 localStorage 到第 2 步：**接受**，与规格一致。
3. Google 真实流程：挂起，待用户配好凭据后浏览器走通再回报。

另注意：你「本地跑法」里写的是 postgresql@16，用户机器实际装的是 **postgresql@17**（且 Homebrew 有个 lib 符号链接坑已排掉），README/HANDOFF 后续统一按 17 表述即可，不影响代码。

---

## [CC → Cowork] 2026-07-06 · 真实用户体系第 1 步完成（users 表 + 认证 + 前端接入）

**后端（backend/，新增 extensions.py / models.py / auth.py / seed.py / migrations/）**
- SQLAlchemy `User` 模型按规格：`id/email(唯一,存小写)/password_hash(可空)/google_sub(可空唯一)/display_name/role/created_at`。Flask-Migrate 脚手架 + 自动生成的迁移 `4c984bc16851_create_users_table.py`，无手写 SQL。
- 认证：Flask-Login httpOnly session cookie（SameSite=Lax，另发 remember cookie），密码 werkzeug scrypt 哈希；服务端校验邮箱格式 + 密码 ≥8。六个端点齐全；失败响应统一 `{error: 稳定码}`（`email_taken`/`invalid_credentials`/`invalid_email`/`password_too_short`/`missing_fields`/`not_authenticated`/`oauth_failed`/`oauth_not_configured`）。
- Google OAuth：Authlib authorization code flow 全后端完成，scope 仅 `openid email profile`；state 由 Authlib 存 session 并在回调校验（CSRF）。回调逻辑按规格：`google_sub` 找→邮箱存在则绑定（补写 google_sub）→否则新建。未配置凭据时点 Google 按钮会**跳回登录页显示翻译后的「未配置」提示**，不会白屏 JSON。
- `seed.py` 幂等：demo `demo@tradelens.app/demo1234`（注意：**demo 密码从 demo123 改为 demo1234**，因最短 8 位）；admin 凭据走 `.env` 的 `ADMIN_EMAIL/ADMIN_PASSWORD`，未配置则跳过并提示。无任何密码/哈希/token 打日志。
- CORS `supports_credentials=True`，仍只允许 5173 两个 origin。`SECRET_KEY` 走 `.env`（未设则随机生成并告警，session 重启失效）。

**前端**
- 新 `services/auth.js`（唯一 auth 通信处，全部 `credentials:'include'`）；`AuthContext` 改为启动时 `GET /me` 恢复会话，login/register/logout 走后端，错误码→i18n 键映射，`ProtectedRoute` 未动。
- 登录/注册页各加「Continue with Google」（`<a>` 顶级导航到 `/api/auth/google`，带内联 G 图标，外部 CSS）；OAuth 失败回跳 `?error=<code>` 会显示翻译文案。
- **localStorage 按用户 id 隔离**：交易 `tl:<userId>:trades`、采用关系 `tl:<userId>:profile`；playbook 库仍全局。两个账号互看不到对方数据。demo 账号首次加载时优先从**旧共享存储迁移** u_1 的交易/采用（保住你之前真实导入的 IB 交易），否则用种子 JSON。
- i18n 新增 7 键 ×7 语言（or/continueGoogle/5 个错误文案），`errPasswordShort` 各语言改为 8 位。

**自检（本机已验）**
- SQLite 临时库上 `flask db upgrade → seed → downgrade → upgrade` 重放通过，seed 幂等。
- **curl 冒烟全链路通过**：me(401)→register(201+cookie)→me(200)→重复邮箱(409 email_taken)→logout→me(401)→错密码(401 invalid_credentials)→demo 大小写混合邮箱登录(200)；Set-Cookie 确认 `HttpOnly; SameSite=Lax`。
- 后端 10 个 .py `py_compile` 通过；7 语言键集 221 键一致、占位符零出入；`vite build` 通过。

**本地跑法（你的机器）**
```bash
# 一次性：Postgres
brew install postgresql@16 && brew services start postgresql@16 && createdb tradelens
# 后端
cd tradelens/backend && source .venv/bin/activate   # venv 已建好，依赖已装
cp .env.example .env    # 填 SECRET_KEY、ADMIN_EMAIL/ADMIN_PASSWORD；Google 凭据可后补
flask --app app db upgrade && python seed.py && python app.py
# 前端
cd tradelens && npm run dev
# 浏览器打开 http://127.0.0.1:5173  ←← 必须用 127.0.0.1，见下
```

**重要坑（请注意）**
- **必须从 `http://127.0.0.1:5173` 打开应用，别用 `localhost:5173`**：session cookie 按 host 存在 `127.0.0.1`（OAuth redirect URI 也定在这），用 localhost 打开浏览器会视为跨站不带 cookie，登录会「成功后立刻掉线」。
- Google 凭据：Google Cloud Console 建「Web application」OAuth client，redirect URI 精确填 `http://127.0.0.1:5001/api/auth/google/callback`。

**不确定点 / 待你拍板**
1. demo 密码 demo123→demo1234（8 位下限所迫），登录页提示已同步。若课堂材料写死了旧密码,告诉我可给 demo 放例外。
2. 采用关系（adoptedPlaybookIds）本步留在前端 per-user localStorage（规格只建 users 表）；第 2 步随 7 张表入库。
3. Google 真实流程需你配好凭据后浏览器走通（新邮箱建号 + 已有邮箱绑定两条路径我按规格实现,但无凭据无法在本机全验）。

---

## [Cowork → CC] 2026-07-06 · 新任务：真实用户体系（M2 正式开工：Postgres + 注册登录 + Google OAuth）

用户已在本机跑通 IB Gateway 实时通路（真账户数据正常，字段无出入）。现在正式进入 Milestone 2：把 mock 登录换成真实用户体系。**用户已拍板：首版同时支持邮箱密码和 Google OAuth。**

**分两步交付，先做第 1 步，验收后再发第 2 步规格。本条只做第 1 步。**

### 第 1 步范围：users 表 + 认证 + 前端接入

**数据库**
- PostgreSQL（用户本机装，连接串走 `backend/.env` 的 `DATABASE_URL`，默认 `postgresql://localhost/tradelens`）。
- SQLAlchemy ORM（Flask-SQLAlchemy）+ Flask-Migrate（Alembic）做迁移，不许手写建表 SQL 散落各处。
- 本步只建 `users` 表：`id`、`email`（唯一、必填）、`password_hash`（可空，纯 OAuth 用户没有）、`google_sub`（可空、唯一）、`display_name`、`role`（`user`/`admin`）、`created_at`。其余 7 张表第 2 步再上。
- 种子脚本：创建 demo 账号（`demo@tradelens.app` / 密码 `demo1234`，role user）和一个 admin 账号（凭据写 `.env`，不硬编码），课堂演示还要用 demo。

**认证方案**
- 会话：Flask-Login + httpOnly session cookie（`SameSite=Lax`），不用 JWT；`SECRET_KEY` 走 `.env`。
- 密码：`werkzeug.security` 的 scrypt/pbkdf2 哈希，绝不存明文；注册时校验邮箱格式 + 密码最短 8 位。
- Google OAuth：Authlib，authorization code flow，全部在后端完成（前端只跳转）。`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` 走 `.env`（用户会自己去 Google Cloud 建凭据，redirect URI 定为 `http://127.0.0.1:5001/api/auth/google/callback`）。回调逻辑：按 `google_sub` 找用户；找不到但邮箱已存在 → 绑定到该账号（补写 google_sub）；都没有 → 新建用户。**只读 profile/email scope，不要多要权限。**
- 端点：`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`、`GET /api/auth/google`（302 到 Google）、`GET /api/auth/google/callback`（成功后 302 回前端）。
- CORS：`supports_credentials=True`，仍只允许 `localhost:5173`；前端 fetch 全部 `credentials: 'include'`。
- 失败响应统一 `{error: <i18n key 可映射的稳定码>}`，如 `email_taken`/`invalid_credentials`，前端据此显示翻译文案，不透传英文原句。

**前端**
- `AuthContext` 从 localStorage mock 改为调上述 API：启动时 `GET /api/auth/me` 恢复会话；登录/注册/登出走后端；`ProtectedRoute` 逻辑不变。
- 登录、注册页各加「Continue with Google」按钮（跳 `GET /api/auth/google`）。
- 新文案全部进 i18n，7 语言齐（界面外壳规则）；键集/占位符校验照旧跑。
- 本步 trades/playbooks 数据**暂时维持 localStorage 现状**（第 2 步才入库迁移），但注意：localStorage 的 key 按用户 id 隔离（如 `tl:<userId>:trades`），避免账号 A 看到账号 B 的本地数据。demo 账号沿用现有 mock 种子数据。

**安全与隐私（硬性）**
- 不打印密码/哈希/token 到日志；`.env` 已在 gitignore；OAuth state 参数必须校验防 CSRF。

**验收**
- 注册 → 登出 → 登录 → 刷新会话保持 → 登出，全链路可用；错误路径（重复邮箱、错密码）有翻译后的提示。
- Google 登录（用户配好凭据后）走通：新邮箱建号、已有邮箱绑定两条路径。
- 两个不同账号登录，互相看不到对方的本地交易数据。
- demo 账号照常可登录、带演示数据。
- 后端 py_compile / 迁移脚本可重放；前端 build 通过；7 语言键集一致。
- 完成后 HANDOFF 顶部追加 `[CC → Cowork]`，附本地跑法（含 Postgres 初始化和 migrate 命令）和不确定点。

---

## [Cowork → CC] 2026-07-06 · P&L 改动审阅通过

独立复验结果：7 语言 `journal.colPnl` 均为 `P&L`；全字典 grep 无 `P&G`/`P&P`/`G&V` 残留；键集 214 × 7 一致、占位符零出入；`vite build` 通过。

你那处超出清单的判断（zh 列头「盈亏」→ `P&L`）**认可**，与「所有语言一致」的决定相符；句中普通名词保留本地语言的处理也正确。本条闭环，无遗留。

---

## [CC → Cowork] 2026-07-06 · P&L 缩写统一改回英文完成

按你 2026-07-06 拍板的决定 1 执行完毕：

**改动（仅缩写场景）**
- `journal.colPnl`（交易历史表列头，独立缩写槽位）：es `P&G`、fr `P&P`、de `G&V`、ja `損益`、ko `손익` → 全部改为 **`P&L`**。
- `reports.totalPnl` 中的仿造缩写：es `P&G realizado total` → `P&L realizado total`、fr `P&P réalisé total` → `P&L réalisé total`、de `Realisierter G&V gesamt` → `Realisierter P&L gesamt`。
- **一处超出你列举清单的判断，请知悉**：zh 的 `colPnl`「盈亏」也在同一缩写槽位，为满足「所有 7 种语言一致」，我把它也改成了 `P&L`。若你想让中文列头保留「盈亏」，一行即可改回。

**按范围说明保留不动**（句中普通名词，以显示自然为准）
- ja `totalPnl`「実現損益合計」、ko「총 실현 손익」、zh「总已实现盈亏」。
- ja/ko/zh 的 `reports.ratio`（損益比/손익비/盈亏比）与 `reports.subtitle` 里的同类表述。

**校验结果**：7 部字典键集与 en 完全一致（各 214 键，零缺失/零多余）；占位符集合逐键比对全部一致。

---

## [Cowork → CC] 2026-07-06 · i18n 七语言 + IB Gateway 两项审阅通过 · 一处术语改动

两项交付均已复核，**审阅通过**。独立验证结果：

**i18n 七语言 + 地球下拉**
- 7 部字典键集与 en 一致（各 214 键），占位符集合逐键比对无出入（独立脚本复验，非仅复述你的自检）。
- `LanguageMenu` 具备 `role=menu`/`menuitemradio`/`aria-checked`/`aria-expanded`，无 inline style，样式走外部 CSS，符合课程规范。旧 `LanguageToggle` 无残留引用。
- 语言注册表模式认可，`tField` 回退英文行为符合规格。

**IB Gateway 只读实时连接**
- 全后端 grep 无任何下单/改单/转账调用（仅注释提及），`readonly=True` 与 `IB_READONLY` 硬编码属实。
- `.gitignore` 覆盖 `.env`、venv、账户导出与 flex xml；`ibkr.js`/`ConnectPage` 无 localStorage 写入，隐私边界符合规格。
- `DataContext.importTrades` 按 `source:externalId` 去重逻辑正确；后端 4 个 .py `py_compile` 通过。
- 前端 `vite build` 通过（Linux 审阅环境缺平台二进制系环境差异，补装后构建成功，与代码无关）。

**用户已拍板的决定（2026-07-06）**
1. **P&L 缩写：统一保留英文 `P&L`**，所有 7 种语言一致。请把 es `P&G`、fr `P&P`、de `G&V`、ja `損益`、ko `손익` 中作为「P&L 缩写」用途的文案改回 `P&L`（仅限缩写场景；句子里作普通名词的「损益」表述不必强改，以显示自然为准）。改完跑一遍键集/占位符校验。
2. 德语保留 Momentum / Mean Reversion / Value 英文术语：**维持现状**。
3. Event-Driven 各语言译法：**接受现译**。
4. `/connect` 作独立顶栏页：**接受**，不挪进 Journal。
5. 导入交易 `playbookId` 置空、用户后补：**接受**。

**仍挂起（不阻塞）**：真实 IB Gateway 账户三端点字段核对，待用户本机连通后反馈；如有字段名出入再修 `ib_service.py` 映射。

---

